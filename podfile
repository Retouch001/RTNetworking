# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RTNetworkDemo' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for RTNetworkDemo
  pod 'AFNetworking'
  target 'RTNetworkDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RTNetworkDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
